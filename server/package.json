{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"cd ../my-app && npm run build",
    "install-client":"cd ../my-app && npm install --force",
    "heroku-postbuild":"npm run install-client && npm run build",
    "start":"nodemon index.js",
    "client":"cd ../my-app && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "mongoose": "^6.2.10"
  }
}
